## 登録用
```javascript
javascript:var _v='jQuery: '+(typeof(jQuery)==='function'?jQuery().jquery:'no use');alert(_v);
```

## 展開
```javascript
var _v = 'jQuery: ' + (
	typeof(jQuery)==='function'
		? jQuery().jquery
		:'no use'
);
alert(_v);
```
